// The frozen test split (PRD section 2). The first one was retired into dev (see golden.ts); the fresh one
// is written in the next commit, before any further tuning.
import type { Golden } from "./golden.ts";

export const TEST: Golden[] = [];
